# アーキテクチャ設計書（変更差分）: [機能名]

## 既存アーキテクチャの概要

<!-- 既存のアーキテクチャを簡潔に記述（変更差分を理解するための文脈） -->

```
[既存のレイヤー構造やモジュール構成を記述]
```

## 変更サマリ

| 変更種別 | 対象 | 内容 |
|---------|------|------|
| 新規追加 | [ファイル/ディレクトリ] | [内容] |
| 変更 | [既存ファイル] | [変更内容] |
| 削除 | [ファイル] | [理由] |

## アーキテクチャ変更の詳細

### 新規追加するレイヤー/コンポーネント

#### [コンポーネント名]

**役割**: [責務の説明]

**配置先**: `[ディレクトリパス]`

**依存関係**:
- → [依存先の既存コンポーネント]（既存）
- ← [依存元の既存コンポーネント]（既存）

### 既存コンポーネントへの変更

#### [既存コンポーネント名]

**変更内容**: [具体的な変更]

**変更理由**: [なぜこの変更が必要か]

**影響範囲**: [この変更が他に影響する箇所]

## ディレクトリ構造の変更

```
existing-repo/
├── [既存ディレクトリ]/
│   ├── [既存ファイル]              # 変更なし
│   ├── [既存ファイル]              # 変更あり ← 変更内容
│   └── [新規ファイル]              # 新規追加
├── [新規ディレクトリ]/              # 新規追加
│   ├── [新規ファイル]
│   └── [新規ファイル]
```

## 新規依存ライブラリ

| ライブラリ | バージョン | 用途 | 選定理由 |
|-----------|-----------|------|----------|
| [名称] | [バージョン] | [用途] | [理由] |

## データ永続化の変更

### 既存スキーマへの変更

```sql
-- 変更: [テーブル名]
ALTER TABLE [table] ADD COLUMN [column] [type];
```

### 新規スキーマ

```sql
CREATE TABLE [table] (
  id UUID PRIMARY KEY,
  [column] [type],
  created_at TIMESTAMP DEFAULT NOW()
);
```

## パフォーマンスへの影響

| 操作 | 現在 | 変更後（見込み） | 対策 |
|------|------|----------------|------|
| [操作1] | [現在の性能] | [変更後の見込み] | [対策] |

## セキュリティへの影響

- [セキュリティ上の変更点]: [対策]

## テスト戦略

### 既存テストへの影響
- [影響を受ける既存テスト]: [対応方法]

### 新規テスト
- [テスト対象]: [テスト方法]

## マイグレーション計画

1. [ステップ1]
2. [ステップ2]
3. [ステップ3]

## ロールバック計画

1. [ステップ1]
2. [ステップ2]

## 技術的リスク

| リスク | 影響度 | 発生確率 | 対策 |
|-------|--------|---------|------|
| [リスク1] | 高/中/低 | 高/中/低 | [対策] |
